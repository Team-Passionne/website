---
import type { BasePropsIcon } from '@utils/interfaces'

export interface Props {
  height?: string | number
  width?: string | number
}

const { height, width } = Astro.props
---

<img src="" width={width} height={height} alt="Logo passionne" />
